"""
CineTrace Insight Engine
========================
Database → Discovery → Ranking → Dedup → Content Generation → Approval → Scheduler.

The LLM never invents facts — discovery modules emit structured Insight objects
(no human language), and generators only convert them into platform copy.
"""
